# Archived Prototype

`archive/Parivahan Sathi Prototype.html` is an earlier single-file export retained only for recovery and design history. It is not the application entry point, is not part of the tested build, and should not be deployed or submitted for evaluation.

The canonical application consists of `index.html`, `styles.css`, `app.js`, `sw.js` and `assets/`.
